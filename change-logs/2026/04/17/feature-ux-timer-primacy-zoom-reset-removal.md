UX overhaul: removed the Reset button (Arm is now multi-state — Готов / Отмена / Новый заезд), reworked the post-ROI layout so the timer dominates the screen while the camera ROI shrinks into a small corner pill. Replaced the initial hardware-camera zoom slider with a pinch-to-zoom + two-finger pan gesture (new viewport.js module applies a CSS transform to the video + overlay), which also works on macOS trackpads via wheel-with-ctrlKey. See decisions/005-pinch-zoom-css-transform.md for rationale.
