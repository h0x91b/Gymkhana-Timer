Documented the hands-free session mode (the new core UX contract: auto re-arm after each run, 15-second cooldown countdown, timer stays on last result until next start) and replaced all "lap timer" wording with "route timer" across README, manifest, package, TZ, AGENTS, and DESIGN. Added a new "Route Timer is king" layout rule at the top of DESIGN.md so future UI work keeps the timer as the single dominant element on screen.
