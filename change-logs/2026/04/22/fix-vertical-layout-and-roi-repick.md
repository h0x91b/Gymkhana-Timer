Fixed the portrait-phone HUD where the hero timer overflowed the viewport (old `clamp(160px, 40vh, 520px)` was height-driven; 5 tabular-serif digits ended up ~2.75× wider than the screen on 360–480 CSS-px phones) and the ROI thumbnail at top-right covered the number. Timer now uses `clamp(96px, min(18vw, 40vh), 360px)` with `max-width: calc(100vw - 32px)`, and the thumbnail anchors bottom-left per AGENTS.md §Session mode. Replaced the two-tap corner picker with "zoom-as-ROI" (`decisions/008`), then evolved it into an aim-mode reticle: a fixed 70vmin green square at screen center, all HUD chrome hidden during framing, only a Confirm ROI button exposed — reticle contents become the ROI via inverse viewport transform (`decisions/010-aim-mode-reticle.md`). Raised `maxZ` 6× → 12× so a phone on a tripod across the lot can still isolate a narrow gate. Added a rising-edge trigger gate to the detector (`decisions/009-rising-edge-trigger.md`) so a subject lingering in the ROI past the 3-second cooldown no longer fabricates a false 3.000s finish.
