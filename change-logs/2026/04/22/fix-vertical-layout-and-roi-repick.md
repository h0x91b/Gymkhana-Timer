Fixed the portrait-phone HUD where the hero timer overflowed the viewport (old `clamp(160px, 40vh, 520px)` was height-driven; 5 tabular-serif digits ended up ~2.75× wider than the screen on 360–480 CSS-px phones) and the ROI thumbnail at top-right covered the number. Timer now uses `clamp(96px, min(18vw, 40vh), 360px)` with `max-width: calc(100vw - 32px)`, and the thumbnail anchors bottom-left per AGENTS.md §Session mode. Also fixed Set ROI being impossible to re-run mid-session: added a `pickingRoi` single-flight guard around `roiPicker.pick()`, hid the controls panel during the pick so its buttons can't absorb the 2nd corner tap, added a translated "tap two opposite corners" hint, and wired a tap on the ROI thumbnail to trigger re-pick directly.
