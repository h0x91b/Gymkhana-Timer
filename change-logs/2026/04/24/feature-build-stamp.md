Added a tiny top-right build stamp that shows the current app version and build timestamp without competing with the route timer. The stamp reads from `build-info.js`; CI overwrites that module with the GitHub Pages publish time and current commit before upload, while local development renders as a dev build. Bumped the service worker cache to `gymkhana-v21` and caches the build-info module so installed PWAs pick up the new stamp.
