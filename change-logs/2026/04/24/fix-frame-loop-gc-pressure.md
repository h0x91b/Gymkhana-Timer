Repro: on weaker Android phones, camera startup begins around 20 FPS and quickly degrades toward 2 FPS during a hands-free session; on current master, ratio can also climb frame-by-frame after start even when the ROI is visually empty. Reduced frame-loop garbage and repaint pressure by reusing detector grayscale/reference buffers, adding early exits to detector scans, throttling ROI thumbnail/debug rendering, caching repeated HUD text writes, cancelling the camera rAF fallback on stop, and subtracting uniform ROI brightness drift before motion comparison.
