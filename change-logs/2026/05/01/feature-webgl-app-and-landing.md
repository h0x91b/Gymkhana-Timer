Restructured the project into a landing page (index.html) that lets the user pick between three builds: the original canvas-based timer (now canvas-app.html), a new GPU-based variant (webgl-app.html, sharing every UI and lifecycle rule with the canvas build via app-webgl.js → webgl-detector.js), and the standalone WebGL benchmark (renamed from webgl-motion-test.html to webgl-bench.html). The new webgl-detector.js mirrors the canvas detector's public API exactly so app.js can swap between them without other changes; pipeline is texImage2D → ROI crop pass → diff vs reference / prev → generateMipmap reduction → readPixels(1×1). Service worker precache list updated to include all new files. Decision record decisions/014 documents the Chrome-on-Android per-process camera lockup that survives every in-app recovery path and only clears on a full Chrome restart.
