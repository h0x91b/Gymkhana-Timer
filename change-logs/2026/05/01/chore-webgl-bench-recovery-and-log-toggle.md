Defensive recovery on the WebGL motion bench page after backgrounding. Re-acquires the camera stream when a video track ends, watches for and reloads on irrecoverable WebGL context loss, and resumes video.play + rVFC if the video paused while the tab was hidden — wired through a single recover() routine triggered by track 'ended' events and visibilitychange='visible'. Log panel is now hidden by default with a Log button to toggle, since the metrics layout was getting cramped on phones.
